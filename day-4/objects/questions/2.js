const obj = {};

obj.push('pat')

console.log(obj);

// What will happen when this file is executed with NodeJS ? undefined
// What is the problem with this code ? .push method will only work on an array
