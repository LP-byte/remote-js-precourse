const person = {name: 'fraz', age: 30};

console.log(pers);

// What will happen when this file is executed with NodeJS ? undefined
// What is the problem with this code ? the wrong variable / variable mispelled is bening called
