const letters = ['a','b','c'];

letters = [1,2,3]
console.log(letters)

// What will happen when this file is executed with NodeJS ? error
// What is the problem with this code ? letters has already been defined as constant